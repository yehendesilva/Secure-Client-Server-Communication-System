# Secure-Client-Server-Communication-System
A multi-client TCP/IP system built in C (server) and Python (client) featuring user authentication with salted password hashing, session token management, rate limiting, brute-force lockout protection after 3 failed attempts, and server-side event logging with timestamps and IP tracking.
